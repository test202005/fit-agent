# fit-agent 当前进度

> 更新时间：2026-09-18（Asia/Shanghai）
> 当前结论：**V4.0 工具调用、V5.0 稳定性与成本口径、V5.1 两模型受控对比、V6 SQLite 持久化、V7 训练计划生成与可观测性均已完成**。
> 下次恢复：先读本文件，再读 [V7 训练计划生成与可观测性迭代复盘](eval/reports/V7%20训练计划生成与可观测性迭代复盘.md)、[两模型受控对比评测报告](eval/reports/两模型受控对比评测报告.md) 与 [问题清单](eval/reports/问题清单.md)。
> 规划依据：[Agent 能力与评测全景](docs/Agent能力与评测全景.md)（按 JD 空白倒推迭代顺序）。

## Prompt 最小治理补强（2026-09-05）

项目没有建设 Prompt 管理平台，只补当前风险需要的版本与追溯能力：六个 Prompt 统一登记在 `backend/prompts/manifest.json`，由 `backend/prompt_registry.py` 加载；运行 Trace 和五套评测报告均记录 Prompt 名称、显式版本与内容 hash。旧 Prompt 文件必须保留，新版本通过 manifest 切换，Git commit 承担 Diff、Review 和回滚。

验证结果（当时口径，后续迭代已升至 192 passed）：150 条单测通过；Intent 58 条 Stub、Extract 28 条 ×2、Query 22 条 ×2、Tool Use 28 条 ×2 全部无 FAIL/ERROR（Intent 原有 2 条 observation 仍为 REVIEW）。本轮没有修改 Prompt 正文，因此不重复运行真实模型质量验收。

**V7 补充（2026-09-16）**：这条「旧 Prompt 文件必须保留」的纪律在 iter-5 真的用上了——`workout_generator` 出 v2 时，v1 原地保留、manifest 改指向 v2，报告的 `generator_prompt_version` 与 `generator_prompt_hash` 随之可区分前后两次 live 运行。

边界：当前没有页面权限、审批流、远程 Prompt 服务、灰度发布或自动回滚；这些属于团队规模扩大后的平台化能力，不包装成已实现。

## 当前状态速览

协作方法入口：[Agentic Coding 协作指南 v0.1](docs/Agentic-Coding协作指南.md)。先建立工作判断框架，再用真实案例逐步补细节；Goal、多 Agent 按实际需要选择。

可观测能力入口：[V7 复盘中的当前能力盘点](eval/reports/V7%20训练计划生成与可观测性迭代复盘.md)。已支持 Case 关联 Trace、过程输入输出核对、计划链路契约检查及版本证据追溯；定位为本地评测排查工具，尚非监控平台。

2026-09-17 13:21：完成时长声明修正，Generator 升 v3，目标时长与未知预计时长拆分，删除无依据凑时长要求；219 条单测通过，五套 Stub 各 2 轮无业务 FAIL/ERROR，计划 Live 7 PASS / 1 REVIEW、112/112 断言。8 条 note 经 AI 助手逐条文本复核未见满足时长的无依据声明，用户语义验收仍待完成。详见 [问题、改法与验证记录](docs/训练计划时长一致性-需求与评测场景初稿.md)。

2026-09-16 11:20 补验：计划 discovery Live（Flash，1 轮）为 7 PASS / 1 REVIEW / 0 FAIL / 0 ERROR，98/98 断言通过，8,104 Token。`pl-004` 未再复制动作；训练时长仍仅校验声明值。详见 [闭环复验](eval/reports/真实BadCase闭环-pl-004.md)，待提交范围见 [ROADMAP](ROADMAP.md)。

| 项 | 状态 |
|---|---|
| 单元测试 | **219 passed**（含时长新协议、断言反例、Trace 字段校验、检测器门禁、源码快照） |
| Stub 全量 | 五套各 2 轮无业务 FAIL/ERROR；每轮意图 56 PASS / 2 REVIEW，抽取 28 PASS，查询 22 PASS，工具 28 PASS，计划 7 PASS / 1 REVIEW；计划另有 2/2 故障检测器自测通过 |
| V5 Flash Live 三视图 ×3 轮 | 四套全部通过；V5.1 双模型差异单列，不与该基线合并 |
| 稳定性与成本口径 | **已关账**；四个 Runner 均有 live 3 轮证据，pass@3 = pass^3 = 1.0000，flaky 0 |
| 两模型受控对比 | **已关账**；Flash 393 PASS / 0 FAIL，Pro 380 PASS / 11 FAIL；默认保持 Flash |
| 架构成本对比 | **已接入**（P-006 已关），12 场景 ×2 轮真实数据 |
| 能力层 | **L1 Tool Use；V7 增加工具结果驱动生成的固定多步链路，尚非自主 ReAct 循环** |
| 仓库 | `github.com/test202005/fit-agent`（PRIVATE），tag v1.0→v4.0 |

## V5.0 本轮（2026-09-03）：稳定性与成本口径

四个 Runner 从「只报单轮通过率」升级到同时报稳定性和成本。

**新增口径**：pass@k（至少一轮通过，峰值能力）、pass^k（全部轮通过，稳定可用）、flaky 名单、高风险 Case 轮轮通过、按模型汇总 token。口径实现在 [eval/stability.py](eval/stability.py)，四个 Runner 共用；`--models` 支持多模型横向对比（仅 live 生效）。

**首轮真实证据**：意图 discovery ×3 轮，`deepseek-v4-flash`，pass@k = pass^k = 1.0000，flaky 0，53,704 token / 577.5 per case。3 条防御用例被 `bad_request` 在模型前短路，零 token。

**对照实验已完成，波动检测证明有效**：同一套集升温到 1.5 重跑 3 轮，pass@k 仍 1.0000 但 pass^k 掉到 0.8621，抓出 4 条 flaky、点名 3 条高风险不稳。并暴露两个单轮观测不到的现象——`intent-adv-006` 在第 1、3 轮把 JSON 注入判成 `record`，`intent-rb-006` 第 3 轮偶发 parse error。升温结果不是质量结论，只证明检测器工作正常。

## V5.0 续（2026-09-04）：口径自测、P-006 关闭、一个被推翻的假设

**口径本身被验证了**：新增 `tests/test_stability_unit.py` 20 条。光有 20 条全绿证明不了什么——温度 0 的全绿数据上，一个「永远宣称无波动」的坏实现和正确实现输出完全相同。所以做了反证：把 `flaky` 改成恒 False、把 pass^k 退化成 pass@k、把 ERROR/REVIEW 计入分母，三种坏实现分别挂掉 5 / 4 / 4 条，还原后 20 passed 且文件零 diff。**这才是这 20 条测试有效的依据。**

**P-006 已关**：`run_architecture_compare.py` 接入 `collect_usages`，两种架构同口径统计调用次数与 token。12 场景 × 2 轮真实模型，两轮 token 数字完全一致。

**实测推翻了 P-006 自己的假设**：

| 架构 | 通过率 | 调用次数/请求 | token/请求 | 平均耗时 |
|---|---:|---:|---:|---:|
| 固定链路 | 0.8333 | 1.80 | 1304.5 | 2084ms |
| 工具调用 | 1.0000 | 1.00 | 1187.2 | 1371ms |

P-006 原文预设「工具描述会进 prompt，成本上升」。实测反过来：工具调用每请求便宜 117 token（-9.0%）、快 713ms（-34%）。原因不是工具调用便宜，是固定链路贵——它每请求要走两次模型（路由 + 抽取/规划），两次 prompt 累加超过了工具 schema 的开销。**只在 3 工具、短 schema 规模成立，不可外推。**

**P-007 / P-008 已关**：Intent Runner 退出码已统一四态契约；Tool Use Runner 每个 trial 改为生成唯一 `trace_id`，修复跨轮结果无法精确回溯的问题。全量单测升至 140 passed。

**V5 关账证据**（详见报告第 8 节）：

1. Extract 28 Case ×3 轮：84 PASS，pass@3 = pass^3 = 1.0000，flaky 0；
2. Query 22 Case ×3 轮：66 PASS，pass@3 = pass^3 = 1.0000，flaky 0；
3. Tool Use 28 Case ×3 轮：84 PASS，pass@3 = pass^3 = 1.0000，flaky 0；
4. 三套新证据的 FAIL / REVIEW / ERROR 和硬门禁违规均为 0；加上已有意图证据，四套正式运行共 360,937 token。

**仍保留的证据边界**：当前数据集不是 blind holdout；尚未覆盖多步/多轮成本；不构成 P95/P99 和并发性能专项。

## V5.1 本轮（2026-09-04）：两模型受控对比

同一提交 `6ead13e`、同一 Case / Prompt / temperature 0，`deepseek-v4-flash` 与 `deepseek-v4-pro` 在四条链路各跑 3 轮。每个模型 399 次执行：Flash 为 393 PASS、6 REVIEW、0 FAIL、0 ERROR；Pro 为 380 PASS、6 REVIEW、11 FAIL、2 ERROR。

Pro 的 3 个 Intent FAIL 是 `intent-rb-010` 连续三轮把 `今天动了动腿` 从 `record` 错分为 `reject`；8 个 Extract FAIL 是 completion 打满 `max_tokens=100` 后 JSON 截断、解析失败。Query 与 Tool Use 两模型全部通过。完整证据和边界见[两模型受控对比评测报告](eval/reports/两模型受控对比评测报告.md)。

## V6 本轮（2026-09-04）：SQLite 持久化与数据一致性

V6 已按 [V6 专项 PRD](docs/prd-v6-persistence.md) 完成，复盘见 [V6 SQLite 持久化迭代复盘](eval/reports/V6 SQLite持久化迭代复盘.md)。默认 HTTP 存储切换为本地 SQLite，FakeStorage 继续用于单测，JsonlStorage 保留为教学对照。

已验证：SQLite 写入与重启恢复、用户范围隔离、同一 `user_id + request_id` 幂等重放、多记录事务回滚、同一业务时间下 UUID 唯一性，以及 HTTP 上下文贯穿。V6 专项测试与既有回归合计 **147 passed**（当时口径；V7 迭代后为 192 passed，见「当前状态速览」）。

边界：`user_id` 目前只是测试上下文，不是认证身份；本轮不宣称完成鉴权、数据库迁移平台或高并发性能专项。

## V7 本轮（2026-09-16）：训练计划生成与可观测性

按 [iter-5 专项 PRD](docs/prd-iter5-plan-generation.md) 实现第 5 条链路 `parse_need` → `call_action_tool` → `parse_workout`，复盘见 [V7 训练计划生成与可观测性迭代复盘](eval/reports/V7%20训练计划生成与可观测性迭代复盘.md)。

**为什么做**：前四条链路都是闭式任务，判分靠比对，黑盒够用。第一条开放式生成链路带来一个新失败模式——**模型理解错了意图，但下游每一步都执行得很正确，最终输出完全合法**。陷阱 case 把「练腿」演成「练胸」，模型带着 `muscle=胸` 查库、拿到真实胸部动作、编出条条合规的计划，黑盒 4/4 全绿；白盒读工具入参，报出「期望 下肢/简单，实际 胸/简单」。

**本轮核心实证已达成**：同一条 case，黑盒校验全绿，白盒抓出问题。新增指标 `blackbox_blind_spots` 记录「黑盒全绿、白盒抓住」的条数。

**同时发现两处真实缺陷**：v1 生成器在 pl-004 把同一条动作抄成 4 行充数，8 条断言一条没红（P-014）；回溯发现 v1 规则 4 把违规写法写成了许可写法，模型还照做并在 note 里复述（P-013）。三处一起修：补断言 `plan_size_within_observation`、Prompt 出 v2、stub 不再重复，`blackbox_blind_spots` 归 0。

**前后对照**：补断言前 v1 断言级 77/77、盲区 0 条（断言集是瞎的）→ 补断言后 v1 83/84、盲区 1 条 pl-004 → 修 Prompt 后 v2 84/84、盲区 0 条。

**证据归属必须分清**：白盒断言的有效性证据来自 stub（陷阱 case 在 live 下不会红，真实模型解析正确）；真实模型的解析能力证据来自 live。两者不能互相顶替。

**边界**：本轮证明的是断言有能力抓「理解错」，不是真实模型一定会犯；动作库是为演示可观测性方法铺的 5 肌群 × 3 难度格，不是可用动作库；pl-006 仍是 REVIEW（排除语义未实现）。PRD 仍是「待评审」，正文未改，两处与实现的差异（case 条数、动作库规模）已在复盘 §5 提请确认。

### 下一轮候选，按全景图排序

1. **L2 继续做厚**——iter-5 已经给出「后一步依赖前一步观察」的产品需求（先查库再编排），链路本身可以再长出「多轮补全需求」「按用户历史调整」等真实需求，但每加一步都要先说清它带来的新风险，不为了展示 ReAct 造伪需求。

**明确不做**：评测平台（等有第二个人要用）、多 Agent（单 Agent 都没做透）、性能专项（等多步链路成型，现在做完上了 ReAct 要重测）。

---

## 历史：2026-09-02 那一轮做了什么

**核心逻辑：先沉淀方法论 → 再用方法论回扫自己 → 扫出问题 → 修 → 记录成版本线。**
不是「做完项目事后总结」，而是「方法论真的抓到了 bug」。

### 新增的方法论（可对外输出）

- [断言方法论](eval/methodology/断言方法论.md)：四问 + 来源分类定尺度 + 四态 Verdict + 三级漏斗（**裁判要做浅：三态、无评分、观察员不是法官**）
- [数据集方法论](eval/methodology/数据集方法论.md)：六步速查 + Golden 准入五条 + Bad Case 回流五步 + 脱敏（**先判断需不需要，再谈怎么脱**）
- 两份都带官方依据，均已脱敏，可直接对外

### 用方法论扫出并修掉的三个洞（V1.1）

1. trace 断言用 `issubset`，防御路径**就算偷偷调了模型也判 PASS** → 改精确集合比对
2. `bad_request` 整条路径没进评测集 → 加 3 条防御 Case，37 → 40 条
3. 响应结构 / 错误码枚举 / 失败泄漏字段**一条都没断** → 新增 `assert_contract()`

验证：单测 24 → **30 passed**，stub 全量 37/37 → **40/40**。

### 本轮最重要的一句结论

> **测试通过 ≠ 测试有效。** V1.0 的 37/37 是真实结果，但其中一条断言在任何情况下都不会失败。
> 一个永远为真的断言，和没有这条断言，信息量完全相同。
> 推论：**评测代码本身也要被验证**——给每条新断言配一个「它应该失败」的用例。

### 其他产出

- `README.md` 重写：定位从「健身记录」改为「**可直接抄走的 Agent 评测脚手架**」
- 全仓脱敏：清掉公司项目数据与本地路径，复扫零命中，具备公开条件
- `content/01-意图识别评测-CSDN主档.md`：系列第一讲成稿

---

## 0.1 下一个小迭代（V1.2）的思路

**按方法论补齐，不追求一次做完。** 详见 [版本演进与问题复盘](eval/reports/版本演进与问题复盘.md)。

**代码侧三件**
1. 第四问·一致性断言：trace 里记录的结论与返回值是否吻合
2. 四态 Verdict：`PASS / FAIL / REVIEW / ERROR`，程序异常与业务失败分账
3. 双口径报告：断言级 + Case 级同时报

**数据侧三件（按性价比排）**
1. 补 `source` 字段——每条 Case 为什么存在、规则变了找谁确认
2. 补 `assertion` 字段——断言目前隐含在 Runner 里，换执行器就丢
3. 补对抗桶输入变体——中英混合、错别字、超长噪声，目前一条没有

### 必须提前留好的两个口子（架构约束，做 V1.2 时不能违反）

**口子一 · trace 要能反推问题，且随项目扩大不失效**

现在是单节点，一个 `trace_id` 串 5 个事件就够。等 iter-2/3 有了 extractor、存储、查询多节点后：

- `trace_id` 必须**贯穿整条链路**，不是每个节点各生成一个
- 每个事件必须带 `node` 字段（已有），排查时能定位到是哪一层歪的
- 多轮出现后要加 `session_id`，与 `trace_id` 分开——一个会话多次调用
- **原则不变**：模型原始输出只进 trace 不进业务返回；trace 本身是被断言对象

**口子二 · 每一层都要能 Mock，不只是 LLM 层**

现在只有 `LLMClient` Protocol 一个注入点，够用是因为只有一层。往后每加一层依赖，都要按同样的形状留口子：

```
LLMClient    →  已有：LiveLLM / StubLLM
Storage      →  iter-2 加：真实 JSONL 落盘 / 内存假实现
Clock        →  涉及「今天/昨天」判定，时间必须可注入，否则用例跨天就飘
```

判据一条：**新增任何外部依赖，如果它让测试变慢、变贵或不确定，就必须能被替换掉。**

---

## 1. 当前处于哪里

```text
Iteration 1 PRD / 架构确认
  → Router、Trace、Stub、Runner、数据集已实现
  → 单测、Stub、DeepSeek Live 评测已执行
  → 实现 Review 的 P0/P1/P2 已整改
  → 候选版本冻结后完成 DeepSeek 干净复验
  → 六条关账门禁关闭，Iteration 1 正式关账
```

当前下一步不是直接造 60 条数据，而是先修订并 Review 新一轮评测计划、数据 schema、迁移规则和 Runner 改造清单。

## 2. 已完成产物

### 产品与研发

- 产品定位：[产品总览](docs/product-overview.md)
- 总体路线：[总体计划](docs/master-plan.md)
- Phase 1 总需求：[Phase 1 PRD](docs/prd.md)
- Iteration 1 业务口径：[意图识别专项 PRD](docs/prd-iter1-intent.md)
- Iteration 1 技术方案：[意图识别架构](docs/architecture-iter1.md)
- Iteration 1 实现讲解（先捋开发再评测）：[迭代一代码实现讲解](docs/迭代一代码实现讲解.md)
- 主人练手路线（练习进度勾选在此）：[Agent开发练手路线](docs/Agent开发练手路线.md)

### 代码

- `backend/router.py`：输入防御、模型调用、解析和统一返回；
- `backend/llm.py`：DeepSeek LiveLLM 与 StubLLM；
- `backend/trace.py`：Router 结构化 Trace；
- `backend/prompts/intent_router_v1.txt`：三分类 Prompt；
- `tests/test_router_unit.py`：Router、Parser、错误路径和 Trace 单测。

### AI 评测

- 测试总入口：[eval/README.md](eval/README.md)
- 新一轮计划：[意图识别评测计划](eval/意图识别评测计划.md)
- 数据集方法：[意图识别数据集设计方法](eval/methodology/意图识别数据集设计方法.md)
- 数据集：`eval/datasets/intent-dataset.jsonl`
- Runner：`eval/run_intent_eval.py`
- Iteration 1 报告：[迭代一意图识别评测报告](eval/reports/迭代一意图识别评测报告.md)
- 原始结果：`eval/results/`，本地保留并被 Git 忽略；
- Trace：`backend/logs/trace.jsonl`，本地保留并被 Git 忽略。

## 3. 已验证结果

最近一次已核对：

| 项目 | 结果 | 当前解释 |
|---|---:|---|
| 单元测试 | 24 passed | 确定性代码契约通过 |
| Stub 全量 | 37/37 passed | 34 条分类 Case + 3 条故障路径通过 |
| DeepSeek discovery | 10 条 × 3 轮，30/30 | 候选版本调试集通过 |
| DeepSeek regression | 8 条 × 3 轮，24/24 | 历史问题回归通过 |
| DeepSeek locked | 16 条 × 3 轮，48/48 | Iteration 1 小规模验收集通过 |

三组 Live 结果均来自运行版本 `1a61947`，报告记录了 Git commit、Prompt/数据集哈希、模型参数及 `confidence_normalized_count=0`。locked 曾参与历史调优，因此只作为 Iteration 1 小规模验收证据，**不宣称为独立 blind holdout，也不据此宣称真实泛化准确率 100%**。

## 4. 实现 Review 关闭结果

权威问题清单：[Iteration 1 实现 Review](docs/iter1-implementation-review.md)。

### P0：版本证据链已补齐

- 已建立基线提交 `0083525`；
- Review 修复提交为 `fc61e67`，契约文档提交为 `ce2d5d1`，最终运行版本为 `1a61947`；
- 报告可回溯当前 Prompt、数据集和采样参数；历史未提交 Prompt 仍不可恢复，已作为历史局限保留，不伪造追溯链。

### P1-1：confidence 解析契约已关闭

- 主人已于 2026-07-19 拍板：保留 0～1 confidence 数字字符串归一化；
- PRD 升级为 v4，并补正式变更记录；
- Runner 已增加 `confidence_normalized_count`，归一化噪声可见；
- 非数字字符串、越界数字字符串和非法 JSON 仍返回 `llm_parse_error`；
- 最终决议与执行清单见 [评测计划 Review](docs/eval-plan-review.md)。

### P1-2：locked 验收口径已关闭

- 调优过程中多次查看 locked 结果；
- 已暴露 Case 虽按规则转入 regression，但最终 locked 仍存在间接适配和选择偏差；
- 本轮已在最终候选版本上一次性跑三轮；报告明确 locked 不是独立 blind holdout；
- 下一轮以新建的 blind holdout 替代受污染 locked，生成、标注、审核和 Prompt 调优角色隔离。

### P2：证据与口径已补齐

- 纯否定 locked Case 已达到 3 条；
- 正式报告已补 Git commit、temperature、max_tokens、thinking、timeout、retries 及哈希；
- `high_risk_into_record` 风险分类口径已校验，本轮三组 Live 结果均为 0；
- 新一轮独立 blind holdout 仍须先过评测计划和数据集 Review，暂未造数。

## 5. 下一步恢复顺序

下次不要直接生成或运行新数据。按以下顺序继续：

1. 把 `docs/eval-plan-review.md` 的五项决议合入评测计划；
2. Review 新 schema、旧 36 条迁移规则和 Runner 改造清单；
3. Review 60 条 blind holdout 覆盖矩阵；
4. 按 2～3 批、每批约 20 条造数；
5. 每批执行主人和独立 AI 双标、分歧处理与泄漏检查；
6. 全部就位并冻结后，才运行 blind holdout 并形成新报告。

## 6. 新一轮评测的当前状态

[意图识别评测计划](eval/意图识别评测计划.md) 已建立，但状态仍是“待 Review”。已经形成的口头候选方案包括：

- 四个评测门禁：范围、数据集、Runner、执行退出；
- Case 同时标记 lifecycle、quality tier、risk type；
- 新一轮优先建设 60 条独立 blind holdout；
- 候选通过标准：macro-F1 ≥ 0.90、每类 Recall ≥ 0.85、parse error 为 0、高风险错进 `record` 为 0；
- 数据集 Review 通过前不开始造数。

上述方向已经拍板，但五项具体决议尚未合入计划正文；合入并逐项 Review 后才成为冻结执行标准。

## 7. Git 与敏感信息状态

- 当前分支：`main`；
- 已建立 Git 版本链；
- Iteration 1 最终运行版本：`1a61947`；
- `.env`、`backend/logs/`、`eval/results/` 已被 `.gitignore` 排除；
- 密钥只允许从 `.env` 读取，不得输出、写入日志或提交。

## 8. 当前事实源优先级

发生冲突时按以下顺序判断：

1. 业务标签和 Router 契约：经主人确认且变更受控的 `docs/prd-iter1-intent.md`；
2. 技术实现约束：`docs/architecture-iter1.md`；
3. 当前 Review 问题：`docs/iter1-implementation-review.md`；
4. 当前恢复状态：本文件；
5. AI 评测执行方法：`eval/意图识别评测计划.md`；
6. 历史结果：`eval/reports/迭代一意图识别评测报告.md` 和 `eval/results/`。

如果 Review 结论与 PRD 冲突，先确认 PRD 变更是否经过主人批准；未经确认的 PRD 修改无效，不直接据此改代码。
