# fit-agent 当前进度

> 更新时间：2026-07-19（Asia/Shanghai）
> 当前结论：Iteration 1 已正式关账；运行版本为 `1a61947`，实现 Review 六条门禁均已关闭。
> 下次恢复：先读本文件，再把 [评测计划 Review](docs/eval-plan-review.md) 的五项决议合入 [意图识别评测计划](eval/意图识别评测计划.md)；计划 Review 前不造新数据。

## 1. 当前处于哪里

```text
Iteration 1 PRD / 架构确认
  → Router、Trace、Stub、Runner、数据集已实现
  → 单测、Stub、DeepSeek Live 评测已执行
  → 实现 Review 的 P0/P1/P2 已整改
  → 候选版本冻结后完成 DeepSeek 干净复验
  → 六条关账门禁关闭，Iteration 1 正式关账
```

当前下一步不是直接造 60 条数据，而是先修订并 Review 新一轮评测计划、数据 schema、迁移规则和 Runner 改造清单。

## 2. 已完成产物

### 产品与研发

- 产品定位：[产品总览](docs/product-overview.md)
- 总体路线：[总体计划](docs/master-plan.md)
- Phase 1 总需求：[Phase 1 PRD](docs/prd.md)
- Iteration 1 业务口径：[意图识别专项 PRD](docs/prd-iter1-intent.md)
- Iteration 1 技术方案：[意图识别架构](docs/architecture-iter1.md)
- Iteration 1 实现讲解（先捋开发再评测）：[迭代一代码实现讲解](docs/迭代一代码实现讲解.md)

### 代码

- `backend/router.py`：输入防御、模型调用、解析和统一返回；
- `backend/llm.py`：DeepSeek LiveLLM 与 StubLLM；
- `backend/trace.py`：Router 结构化 Trace；
- `backend/prompts/intent_router_v1.txt`：三分类 Prompt；
- `tests/test_router_unit.py`：Router、Parser、错误路径和 Trace 单测。

### AI 评测

- 测试总入口：[eval/README.md](eval/README.md)
- 新一轮计划：[意图识别评测计划](eval/意图识别评测计划.md)
- 数据集方法：[意图识别数据集设计方法](eval/methodology/意图识别数据集设计方法.md)
- 数据集：`eval/datasets/intent-dataset.jsonl`
- Runner：`eval/run_intent_eval.py`
- Iteration 1 报告：[迭代一意图识别评测报告](eval/reports/迭代一意图识别评测报告.md)
- 原始结果：`eval/results/`，本地保留并被 Git 忽略；
- Trace：`backend/logs/trace.jsonl`，本地保留并被 Git 忽略。

## 3. 已验证结果

最近一次已核对：

| 项目 | 结果 | 当前解释 |
|---|---:|---|
| 单元测试 | 24 passed | 确定性代码契约通过 |
| Stub 全量 | 37/37 passed | 34 条分类 Case + 3 条故障路径通过 |
| DeepSeek discovery | 10 条 × 3 轮，30/30 | 候选版本调试集通过 |
| DeepSeek regression | 8 条 × 3 轮，24/24 | 历史问题回归通过 |
| DeepSeek locked | 16 条 × 3 轮，48/48 | Iteration 1 小规模验收集通过 |

三组 Live 结果均来自运行版本 `1a61947`，报告记录了 Git commit、Prompt/数据集哈希、模型参数及 `confidence_normalized_count=0`。locked 曾参与历史调优，因此只作为 Iteration 1 小规模验收证据，**不宣称为独立 blind holdout，也不据此宣称真实泛化准确率 100%**。

## 4. 实现 Review 关闭结果

权威问题清单：[Iteration 1 实现 Review](docs/iter1-implementation-review.md)。

### P0：版本证据链已补齐

- 已建立基线提交 `0083525`；
- Review 修复提交为 `fc61e67`，契约文档提交为 `ce2d5d1`，最终运行版本为 `1a61947`；
- 报告可回溯当前 Prompt、数据集和采样参数；历史未提交 Prompt 仍不可恢复，已作为历史局限保留，不伪造追溯链。

### P1-1：confidence 解析契约已关闭

- 主人已于 2026-07-19 拍板：保留 0～1 confidence 数字字符串归一化；
- PRD 升级为 v4，并补正式变更记录；
- Runner 已增加 `confidence_normalized_count`，归一化噪声可见；
- 非数字字符串、越界数字字符串和非法 JSON 仍返回 `llm_parse_error`；
- 最终决议与执行清单见 [评测计划 Review](docs/eval-plan-review.md)。

### P1-2：locked 验收口径已关闭

- 调优过程中多次查看 locked 结果；
- 已暴露 Case 虽按规则转入 regression，但最终 locked 仍存在间接适配和选择偏差；
- 本轮已在最终候选版本上一次性跑三轮；报告明确 locked 不是独立 blind holdout；
- 下一轮以新建的 blind holdout 替代受污染 locked，生成、标注、审核和 Prompt 调优角色隔离。

### P2：证据与口径已补齐

- 纯否定 locked Case 已达到 3 条；
- 正式报告已补 Git commit、temperature、max_tokens、thinking、timeout、retries 及哈希；
- `high_risk_into_record` 风险分类口径已校验，本轮三组 Live 结果均为 0；
- 新一轮独立 blind holdout 仍须先过评测计划和数据集 Review，暂未造数。

## 5. 下一步恢复顺序

下次不要直接生成或运行新数据。按以下顺序继续：

1. 把 `docs/eval-plan-review.md` 的五项决议合入评测计划；
2. Review 新 schema、旧 36 条迁移规则和 Runner 改造清单；
3. Review 60 条 blind holdout 覆盖矩阵；
4. 按 2～3 批、每批约 20 条造数；
5. 每批执行主人和独立 AI 双标、分歧处理与泄漏检查；
6. 全部就位并冻结后，才运行 blind holdout 并形成新报告。

## 6. 新一轮评测的当前状态

[意图识别评测计划](eval/意图识别评测计划.md) 已建立，但状态仍是“待 Review”。已经形成的口头候选方案包括：

- 四个评测门禁：范围、数据集、Runner、执行退出；
- Case 同时标记 lifecycle、quality tier、risk type；
- 新一轮优先建设 60 条独立 blind holdout；
- 候选通过标准：macro-F1 ≥ 0.90、每类 Recall ≥ 0.85、parse error 为 0、高风险错进 `record` 为 0；
- 数据集 Review 通过前不开始造数。

上述方向已经拍板，但五项具体决议尚未合入计划正文；合入并逐项 Review 后才成为冻结执行标准。

## 7. Git 与敏感信息状态

- 当前分支：`main`；
- 已建立 Git 版本链；
- Iteration 1 最终运行版本：`1a61947`；
- `.env`、`backend/logs/`、`eval/results/` 已被 `.gitignore` 排除；
- 密钥只允许从 `.env` 读取，不得输出、写入日志或提交。

## 8. 当前事实源优先级

发生冲突时按以下顺序判断：

1. 业务标签和 Router 契约：经主人确认且变更受控的 `docs/prd-iter1-intent.md`；
2. 技术实现约束：`docs/architecture-iter1.md`；
3. 当前 Review 问题：`docs/iter1-implementation-review.md`；
4. 当前恢复状态：本文件；
5. AI 评测执行方法：`eval/意图识别评测计划.md`；
6. 历史结果：`eval/reports/迭代一意图识别评测报告.md` 和 `eval/results/`。

如果 Review 结论与 PRD 冲突，先确认 PRD 变更是否经过主人批准；未经确认的 PRD 修改无效，不直接据此改代码。
