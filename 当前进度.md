# fit-agent 当前进度

> 更新时间：2026-07-19（Asia/Shanghai）
> 当前结论：Iteration 1 功能实现和评测闭环已完成，但实现 Review 发现 P0/P1 问题，**暂未正式关账，不进入 Iteration 2**。
> 下次恢复：先读本文件，再读 [Iteration 1 实现 Review](docs/iter1-implementation-review.md)。

## 1. 当前处于哪里

```text
Iteration 1 PRD / 架构确认
  → Router、Trace、Stub、Runner、数据集已实现
  → 单测、Stub、DeepSeek Live 评测已执行
  → 实现 Review 已完成
  → 当前停在 Review 问题整改前
  → P0/P1/P2 处理并干净复验后才能关账
```

当前不是“还没做完功能”，而是“功能已完成，版本证据链和验收可信度还没完成”。

## 2. 已完成产物

### 产品与研发

- 产品定位：[产品总览](docs/product-overview.md)
- 总体路线：[总体计划](docs/master-plan.md)
- Phase 1 总需求：[Phase 1 PRD](docs/prd.md)
- Iteration 1 业务口径：[意图识别专项 PRD](docs/prd-iter1-intent.md)
- Iteration 1 技术方案：[意图识别架构](docs/architecture-iter1.md)

### 代码

- `backend/router.py`：输入防御、模型调用、解析和统一返回；
- `backend/llm.py`：DeepSeek LiveLLM 与 StubLLM；
- `backend/trace.py`：Router 结构化 Trace；
- `backend/prompts/intent_router_v1.txt`：三分类 Prompt；
- `tests/test_router_unit.py`：Router、Parser、错误路径和 Trace 单测。

### AI 评测

- 测试总入口：[eval/README.md](eval/README.md)
- 新一轮计划：[意图识别评测计划](eval/意图识别评测计划.md)
- 数据集方法：[意图识别数据集设计方法](eval/methodology/意图识别数据集设计方法.md)
- 数据集：`eval/datasets/intent-dataset.jsonl`
- Runner：`eval/run_intent_eval.py`
- Iteration 1 报告：[迭代一意图识别评测报告](eval/reports/迭代一意图识别评测报告.md)
- 原始结果：`eval/results/`，本地保留并被 Git 忽略；
- Trace：`backend/logs/trace.jsonl`，本地保留并被 Git 忽略。

## 3. 已验证结果

最近一次已核对：

| 项目 | 结果 | 当前解释 |
|---|---:|---|
| 单元测试 | 22 passed | 确定性代码契约通过 |
| Stub 全量 | 36/36 passed | Router、Parser、Trace、Runner 和故障路径通过 |
| 当前 locked 历史结果 | 15 条 × 3 轮全过 | 只能证明小规模验收集通过，不代表真实泛化准确率 100% |

当前报告数字不能直接作为正式关账证据，原因见实现 Review：locked 曾在 Prompt 调优过程中多次运行，且版本没有 Git commit 支撑，历史 Prompt 内容无法还原。

## 4. 尚未关闭的问题

权威问题清单：[Iteration 1 实现 Review](docs/iter1-implementation-review.md)。

### P0：版本证据链断裂

- Git 仓库仍是零 commit；
- 当前全部项目文件处于 untracked 状态；
- 历史评测报告有多个 `prompt_hash`，但无法从 Git 还原对应 Prompt 原文；
- 需要补建当前版本基线，并规定后续 Prompt 修改单独 commit。

### P1-1：confidence 解析契约需要重新对齐

- 当前代码接受数字字符串 confidence；
- 实现 Review 认为这与严格解析叙事存在矛盾；
- 但专项 PRD v3 明确写了“允许将 0～1 数字字符串归一化为 number”，因此**动代码前必须先以 PRD 为准重新 Review 这条结论**；
- 不允许直接照 Review 文档删除兼容逻辑，避免把符合 PRD 的实现误改成不符合 PRD。

### P1-2：locked 使用方式削弱验收可信度

- 调优过程中多次查看 locked 结果；
- 已暴露 Case 虽按规则转入 regression，但最终 locked 仍存在间接适配和选择偏差；
- 新规则应明确：调优只跑 discovery/regression；locked 只在最终候选版本上一次性跑三轮；补位 Case 由未参与 Prompt 调优的人盲写。

### P2

- 纯否定 Case 数量需要按 PRD 重新统计，确认是否达到 ≥3；
- 正式报告需要补 temperature 等采样参数；
- `high_risk_into_record` 的风险分类口径需要逐行 Review；
- 新一轮独立 blind holdout 的数据集设计尚未 Review 通过，暂未造数。

## 5. 下一步恢复顺序

下次不要直接跑新评测，也不要直接开始 Iteration 2。按以下顺序继续：

1. 对照 PRD、架构和实现 Review，重新核对 P1-1 confidence 契约；
2. Review 并写入 locked 只用于最终验收的规则；
3. 统计数据集覆盖，确认纯否定 Case 数量和风险标签口径；
4. Runner 报告补齐 temperature / 采样参数；
5. 建立当前 Git 基线和 Prompt 版本规则；
6. 跑单测与 Stub 全量；
7. 如代码或 Prompt 有变化，先跑 discovery/regression；
8. 冻结候选版本后一次性执行 locked 三轮；
9. 更新正式报告与实现 Review，六条关账门禁全部勾选；
10. Iteration 1 正式关账后，再 Review 新一轮 60 条 blind holdout 数据集设计。

## 6. 新一轮评测的当前状态

[意图识别评测计划](eval/意图识别评测计划.md) 已建立，但状态仍是“待 Review”。已经形成的口头候选方案包括：

- 四个评测门禁：范围、数据集、Runner、执行退出；
- Case 同时标记 lifecycle、quality tier、risk type；
- 新一轮优先建设 60 条独立 blind holdout；
- 候选通过标准：macro-F1 ≥ 0.90、每类 Recall ≥ 0.85、parse error 为 0、高风险错进 `record` 为 0；
- 数据集 Review 通过前不开始造数。

这些仍是待主人 Review 的候选方案，不是已经冻结的执行标准。

## 7. Git 与敏感信息状态

- 当前分支：`main`；
- 当前没有任何 commit；
- 所有项目文件仍为 untracked；
- `.env`、`backend/logs/`、`eval/results/` 已被 `.gitignore` 排除；
- 密钥只允许从 `.env` 读取，不得输出、写入日志或提交。

## 8. 当前事实源优先级

发生冲突时按以下顺序判断：

1. 业务标签和 Router 契约：`docs/prd-iter1-intent.md`；
2. 技术实现约束：`docs/architecture-iter1.md`；
3. 当前 Review 问题：`docs/iter1-implementation-review.md`；
4. 当前恢复状态：本文件；
5. AI 评测执行方法：`eval/意图识别评测计划.md`；
6. 历史结果：`eval/reports/迭代一意图识别评测报告.md` 和 `eval/results/`。

如果 Review 结论与 PRD 冲突，先修正 Review 或回到业务决策，不直接改代码。
